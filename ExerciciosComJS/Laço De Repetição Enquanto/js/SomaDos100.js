function calculo() {
    let soma = 0;
    let contadora =  1;
    while (contadora <101) {
    soma = Number (soma + contadora)
     console.log('A soma dos 100 primeiros números é: ' + soma)
     contadora ++
    }
}