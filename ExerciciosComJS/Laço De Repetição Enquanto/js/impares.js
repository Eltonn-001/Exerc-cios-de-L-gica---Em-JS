function impares() {
    let contadora = 1;
    while (contadora<21) {
        if (contadora%2!=0) {
            console.log(contadora + "É impar.")
        
        }
        contadora ++
    }
}